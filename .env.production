# Production environment variables
# These are safe defaults that prevent secrets from being bundled

# Appwrite Configuration (should be set in Netlify environment variables)
VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=

# Do not include any actual API keys or secrets in this file
# All sensitive values should be configured in Netlify's environment variables